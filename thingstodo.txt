1) Add mixing capabilities and binary operations! Like, (~in1 + ~in2) => ~out
2) interpolationProxies should be created everytime a new source is declared
3) Pbinds are still buggy, and their parameters are always considered kr... fix this